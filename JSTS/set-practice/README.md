# Set Practice

This folder contains JavaScript and TypeScript examples for working with Sets:

- add
- has
- delete
- clear
- size
- values
- entries

Run the JavaScript file with:

```bash
node set-practice.js
```

Compile the TypeScript file with:

```bash
tsc set-practice.ts
```
